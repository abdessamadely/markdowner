## Documentation

<pre>
markdowner
  props
    value: EditorBlock[]
    onChange: (id: string, newValue: EditorBlock)
  blocks
    paragraph
      index.ts
      paragraph.utils
      paragraph.types.ts
      paragraph.input.tsx
      paragraph.renderer.ts
</pre>

## TODO:

- [ ] If a block is an input or textarea, move focus to previous block when delete key is clicked and remove the empty block.
